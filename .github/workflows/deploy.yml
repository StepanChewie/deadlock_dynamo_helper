name: Deploy API

on:
  push:
    branches:
      - main
    # docs/ is already excluded from the rsync step below and the Dockerfile
    # copies no .md files, so a docs-only commit cannot change the image.
    # Without this filter every README tweak cost a full arm64 rebuild.
    paths-ignore:
      - 'docs/**'
      - '**/*.md'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-api-production
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: [self-hosted, Linux, Arm64]
    timeout-minutes: 60
    env:
      ADAPTIVE_PRODUCTION_IMAGE: deadlock-adaptive-production:${{ github.sha }}
      CHROMIUM_PATH: /usr/bin/chromium-browser
      PUBLIC_API_BASE_URL: ${{ vars.PUBLIC_API_BASE_URL || 'https://aboba-telegramovich.duckdns.org' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Copy files to deployment directory
        run: |
          rsync -avz --delete \
            --exclude 'node_modules' \
            --exclude '.env' \
            --exclude 'storage' \
            --exclude '.git' \
            --exclude '.github' \
            --exclude 'docs' \
            --exclude 'training' \
            ./ /home/ubuntu/apps/deadlock_dynamo_helper/

      - name: Rebuild and restart adaptive API
        run: |
          set -euo pipefail
          cd /home/ubuntu/apps/deadlock_dynamo_helper
          sudo docker build --platform linux/arm64 \
            --label "org.opencontainers.image.revision=${GITHUB_SHA}" \
            -t "$ADAPTIVE_PRODUCTION_IMAGE" .
          sudo env \
            DEADLOCK_API_IMAGE="$ADAPTIVE_PRODUCTION_IMAGE" \
            CHROMIUM_PATH="$CHROMIUM_PATH" \
            docker compose run --rm --no-deps api node run-migrations.js
          sudo env \
            DEADLOCK_API_IMAGE="$ADAPTIVE_PRODUCTION_IMAGE" \
            DB_RUN_MIGRATIONS='false' \
            CHROMIUM_PATH="$CHROMIUM_PATH" \
            docker compose up -d --force-recreate --no-build --no-deps api

      - name: Wait for healthy API container
        shell: bash
        run: |
          set -euo pipefail
          cd /home/ubuntu/apps/deadlock_dynamo_helper
          container_id="$(sudo docker compose ps -q api)"
          if [ -z "$container_id" ]; then
            echo 'API container was not created.' >&2
            exit 1
          fi

          sudo bash scripts/verify-deployed-api-image.sh \
            "$GITHUB_SHA" \
            "$container_id" \
            "$ADAPTIVE_PRODUCTION_IMAGE"

          for attempt in $(seq 1 40); do
            status="$(sudo docker inspect --format='{{if .State.Health}}{{.State.Health.Status}}{{else}}missing{{end}}' "$container_id")"
            echo "API health status: $status ($attempt/40)"
            if [ "$status" = 'healthy' ]; then
              exit 0
            fi
            if [ "$status" = 'unhealthy' ]; then
              sudo docker compose logs --tail=200 api
              exit 1
            fi
            sleep 5
          done

          sudo docker compose logs --tail=200 api
          echo 'API did not become healthy in time.' >&2
          exit 1

      - name: Install ops units (health watcher and database backup)
        run: |
          set -euo pipefail
          cd /home/ubuntu/apps/deadlock_dynamo_helper

          # These scripts and units are versioned here, so install them from the
          # checkout on every deploy. Config lives in /etc/*.env and is never
          # touched by this step: the webhook URL and thresholds are server
          # state, not repository state.
          #
          # Before this step existed, ops/ was rsynced but never installed, so
          # a fix to the watcher reached the server and did nothing until
          # someone copied it by hand -- which is how a stale build stayed
          # deployed once already.
          sudo install -m 0755 ops/monitoring/deadlock-health-watch.sh /usr/local/bin/
          sudo install -m 0755 ops/monitoring/deadlock-data-freshness-watch.sh /usr/local/bin/
          sudo install -m 0755 ops/backups/deadlock-db-backup.sh       /usr/local/bin/
          sudo install -m 0755 ops/nginx/check-drift.sh                /usr/local/bin/deadlock-nginx-drift-check.sh
          sudo install -m 0644 ops/monitoring/deadlock-health-watch.service /etc/systemd/system/
          sudo install -m 0644 ops/monitoring/deadlock-health-watch.timer   /etc/systemd/system/
          sudo install -m 0644 ops/monitoring/deadlock-data-freshness-watch.service /etc/systemd/system/
          sudo install -m 0644 ops/monitoring/deadlock-data-freshness-watch.timer   /etc/systemd/system/
          sudo install -m 0644 ops/backups/deadlock-db-backup.service       /etc/systemd/system/
          sudo install -m 0644 ops/backups/deadlock-db-backup.timer         /etc/systemd/system/

          # The nginx snapshot is the baseline the drift check compares against,
          # so it has to live on the host too. Kept outside /usr/local/bin
          # because it is data, not an executable.
          sudo install -d -m 0755 /usr/local/share/deadlock
          sudo install -m 0644 ops/nginx/aboba-telegramovich.duckdns.org.conf \
            /usr/local/share/deadlock/nginx-snapshot.conf

          sudo systemctl daemon-reload
          sudo systemctl enable --now deadlock-health-watch.timer
          sudo systemctl enable --now deadlock-data-freshness-watch.timer
          sudo systemctl enable --now deadlock-db-backup.timer

          # Assert the install happened instead of trusting it: a copy that
          # silently did nothing is exactly the failure this step exists for.
          cmp -s ops/monitoring/deadlock-health-watch.sh /usr/local/bin/deadlock-health-watch.sh
          cmp -s ops/monitoring/deadlock-data-freshness-watch.sh /usr/local/bin/deadlock-data-freshness-watch.sh
          cmp -s ops/backups/deadlock-db-backup.sh       /usr/local/bin/deadlock-db-backup.sh
          cmp -s ops/nginx/check-drift.sh                /usr/local/bin/deadlock-nginx-drift-check.sh
          cmp -s ops/nginx/aboba-telegramovich.duckdns.org.conf /usr/local/share/deadlock/nginx-snapshot.conf
          systemctl is-active --quiet deadlock-health-watch.timer
          systemctl is-active --quiet deadlock-data-freshness-watch.timer
          systemctl is-active --quiet deadlock-db-backup.timer
          echo 'ops units installed; all timers active'

          # Run the freshness watch once, right now, and report what it found.
          # Deliberately non-fatal: a stale dataset is a symptom to investigate,
          # not a reason to fail a deploy that otherwise succeeded. Making it
          # gate the pipeline would turn a reporting signal into an outage path
          # -- the same mistake as putting a rate limiter on a healthcheck.
          if sudo /usr/local/bin/deadlock-data-freshness-watch.sh; then
            echo 'build data is fresh'
          else
            echo "::warning title=build data is stale::deadlock-data-freshness-watch reported a problem. Read the output above; the same finding has been alerted if a webhook is configured."
          fi

      - name: Verify Statlocker adaptive serving locally
        run: |
          set -euo pipefail
          cd /home/ubuntu/apps/deadlock_dynamo_helper
          sudo docker compose exec -T api node -e "fetch('http://127.0.0.1:3000/deadlock/adaptive/v1/status').then(async (response) => { const body = await response.text(); console.log(body); if (!response.ok) process.exit(1); }).catch((error) => { console.error(error); process.exit(1); });"
          sudo docker compose exec -T api node -e "Promise.all([fetch('http://127.0.0.1:3000/deadlock/analysis/build-recommendation',{method:'POST',headers:{'content-type':'application/json'},body:'{}'}),fetch('http://127.0.0.1:3000/deadlock/analysis/contextual-v3-live',{method:'POST',headers:{'content-type':'application/json'},body:'{}'}),fetch('http://127.0.0.1:3000/deadlock/analysis/heroes/1/skill-build')]).then(async (responses) => { const codes = responses.map((response) => response.status); console.log(JSON.stringify(codes)); if (codes.some((code) => code !== 404)) process.exit(1); }).catch((error) => { console.error(error); process.exit(1); });"

      - name: Restore DuckDNS record when needed
        shell: bash
        env:
          DUCKDNS_TOKEN: ${{ secrets.DUCKDNS_TOKEN }}
        run: |
          set -euo pipefail
          public_host="${PUBLIC_API_BASE_URL#https://}"
          public_host="${public_host%%/*}"
          public_host="${public_host%%:*}"

          if [[ "$public_host" != *.duckdns.org ]]; then
            echo "Public host $public_host is not managed by DuckDNS."
            exit 0
          fi

          resolve_public_host() {
            getent ahostsv4 "$public_host" | awk 'NR == 1 { print $1 }'
          }

          resolved_ip="$(resolve_public_host || true)"
          if [ -n "$resolved_ip" ]; then
            echo "DuckDNS already resolves $public_host to $resolved_ip."
            exit 0
          fi

          duckdns_token="${DUCKDNS_TOKEN:-}"
          for env_file in \
            /home/ubuntu/apps/deadlock_dynamo_helper/.env \
            /home/ubuntu/apps/aboba-telegramovich/.env \
            /etc/duckdns/duckdns.env; do
            if [ -n "$duckdns_token" ] || [ ! -r "$env_file" ]; then
              continue
            fi
            duckdns_token="$(sed -n 's/^DUCKDNS_TOKEN=//p' "$env_file" | tail -n 1 | tr -d '\r' | sed -e 's/^"//' -e 's/"$//' -e "s/^'//" -e "s/'$//")"
          done

          if [ -z "$duckdns_token" ]; then
            echo 'DuckDNS does not resolve and DUCKDNS_TOKEN is unavailable.' >&2
            exit 1
          fi

          duckdns_domain="${public_host%.duckdns.org}"
          update_result="$(curl --fail --show-error --silent --get \
            'https://www.duckdns.org/update' \
            --data-urlencode "domains=$duckdns_domain" \
            --data-urlencode "token=$duckdns_token" \
            --data-urlencode 'ip=')"
          if [ "$update_result" != 'OK' ]; then
            echo "DuckDNS update failed: $update_result" >&2
            exit 1
          fi

          for attempt in $(seq 1 30); do
            resolved_ip="$(resolve_public_host || true)"
            if [ -n "$resolved_ip" ]; then
              echo "DuckDNS resolves $public_host to $resolved_ip."
              exit 0
            fi
            echo "Waiting for DuckDNS propagation ($attempt/30)."
            sleep 5
          done

          echo "DuckDNS update succeeded but $public_host still does not resolve." >&2
          exit 1

      - name: Configure nginx Deadlock route
        shell: bash
        run: |
          set -euo pipefail
          public_host="${PUBLIC_API_BASE_URL#https://}"
          public_host="${public_host%%/*}"
          public_host="${public_host%%:*}"

          sudo rm -f "/etc/nginx/sites-enabled/${public_host}.before-deadlock-route"

          sudo env PUBLIC_API_HOST="$public_host" python3 - <<'PY'
          import os
          import re
          import shutil
          from pathlib import Path

          host = os.environ['PUBLIC_API_HOST']
          config_path = Path('/etc/nginx/sites-enabled') / host
          if not config_path.exists():
              raise SystemExit(f'Nginx site config does not exist: {config_path}')

          source = config_path.read_text()
          marker_start = '    # BEGIN DEADLOCK_DYNAMO_HELPER\n'
          marker_end = '    # END DEADLOCK_DYNAMO_HELPER\n'
          location_block = (
              marker_start
              + '    location ^~ /deadlock/ {\n'
              + '        proxy_pass http://127.0.0.1:3000;\n'
              + '        proxy_http_version 1.1;\n'
              + '        proxy_set_header Host $host;\n'
              + '        proxy_set_header X-Real-IP $remote_addr;\n'
              + '        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n'
              + '        proxy_set_header X-Forwarded-Proto $scheme;\n'
              + '        proxy_read_timeout 60s;\n'
              + '    }\n'
              + marker_end
          )

          if marker_start in source and marker_end in source:
              pattern = re.compile(re.escape(marker_start) + r'.*?' + re.escape(marker_end), re.DOTALL)
              updated = pattern.sub(location_block, source, count=1)
          else:
              server_pattern = re.compile(r'\bserver\s*\{')
              updated = None
              for match in server_pattern.finditer(source):
                  open_brace = source.find('{', match.start())
                  depth = 0
                  close_brace = None
                  for index in range(open_brace, len(source)):
                      character = source[index]
                      if character == '{':
                          depth += 1
                      elif character == '}':
                          depth -= 1
                          if depth == 0:
                              close_brace = index
                              break
                  if close_brace is None:
                      continue
                  server_block = source[match.start():close_brace + 1]
                  if re.search(rf'\bserver_name\s+{re.escape(host)}\s*;', server_block) and (re.search(r'\blisten\s+443\b', server_block) or 'ssl_certificate' in server_block):
                      updated = source[:close_brace] + '\n' + location_block + source[close_brace:]
                      break
              if updated is None:
                  raise SystemExit(f'HTTPS nginx server block was not found for {host}.')

          if updated != source:
              backup_dir = Path('/var/backups/nginx')
              backup_dir.mkdir(parents=True, exist_ok=True)
              backup_path = backup_dir / f'{host}.before-deadlock-route'
              if not backup_path.exists():
                  shutil.copy2(config_path, backup_path)
              config_path.write_text(updated)
              print(f'Updated nginx route in {config_path}.')
          else:
              print('Nginx Deadlock route is already up to date.')
          PY

          sudo nginx -t
          sudo systemctl reload nginx

      # Deliberately non-fatal, and deliberately after the route is applied.
      #
      # The live vhost is NOT deployed from this repository -- this step's
      # predecessor rewrites one block inside it, by hand-applied edits made the
      # other two, and Certbot rewrites the ssl_* lines on every renewal. So the
      # file drifts, and before this step nothing reported it: "what is exposed
      # to the internet" could change without a commit and without a failure.
      #
      # It must not gate the deploy. Drift is expected, and a red pipeline over
      # a certificate renewal would be a reporting gap turned into an outage
      # path -- the same mistake as putting a rate limiter on a healthcheck.
      - name: Report nginx perimeter drift (non-fatal)
        continue-on-error: true
        run: |
          set -uo pipefail
          if /usr/local/bin/deadlock-nginx-drift-check.sh; then
            echo 'nginx perimeter is in sync with the committed snapshot'
          else
            echo "::warning title=nginx perimeter drift::The live nginx config differs from ops/nginx/aboba-telegramovich.duckdns.org.conf. Read the diff above, and refresh the snapshot only if the change is intended."
          fi

      - name: Verify Statlocker adaptive through local HTTPS
        shell: bash
        run: |
          set -euo pipefail
          public_host="${PUBLIC_API_BASE_URL#https://}"
          public_host="${public_host%%/*}"
          public_host="${public_host%%:*}"
          curl --fail --show-error --silent \
            --retry 10 \
            --retry-delay 1 \
            --retry-all-errors \
            --resolve "$public_host:443:127.0.0.1" \
            "$PUBLIC_API_BASE_URL/deadlock/adaptive/v1/status"

      - name: Verify Statlocker adaptive public route
        shell: bash
        run: |
          set -euo pipefail
          curl --fail --show-error --silent \
            --retry 10 \
            --retry-delay 3 \
            --retry-all-errors \
            "$PUBLIC_API_BASE_URL/deadlock/adaptive/v1/status"

          legacy_status="$(curl --show-error --silent --output /dev/null --write-out '%{http_code}' \
            -X POST \
            -H 'content-type: application/json' \
            --data '{}' \
            "$PUBLIC_API_BASE_URL/deadlock/analysis/build-recommendation")"
          if [ "$legacy_status" != '404' ]; then
            echo "Legacy build recommendation route is still enabled: HTTP $legacy_status" >&2
            exit 1
          fi

      - name: Record verified adaptive Compose fallback
        shell: bash
        run: |
          set -euo pipefail
          sudo docker tag "$ADAPTIVE_PRODUCTION_IMAGE" deadlock-adaptive-production:current

      - name: Show API logs on failure
        if: failure()
        run: |
          cd /home/ubuntu/apps/deadlock_dynamo_helper
          sudo docker compose ps
          sudo docker compose logs --tail=300 api
