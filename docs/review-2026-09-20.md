# Ревью перед подачей в Overwolf Appstore — 2026-09-20

Продолжение `docs/review-2026-09-17.md`. То ревью нашло O1–O11; здесь проверено, **что из
них реально закрыто**, и добавлено то, что изменилось за три дня.

Всё ниже проверено фактами: прогонами, запросами к проду и чтением файлов. Не по документам —
документы датированы 17-м и в двух местах уже разошлись с кодом.

---

## 1. Состояние на сегодня

| Гейт | Проверка | Результат |
|---|---|---|
| Тесты | `yarn test` | **834 зелёных** (108 + 33 + 13 сюит), 6 пропущено |
| Сборка клиента | `OVERWOLF_API_BASE_URL=… yarn build` | **exit 0**, валидатор проходит |
| Адрес API в бандле | `grep duckdns public/dist/index.js` | есть, плейсхолдера нет |
| Валидатор | `validate-release.js` | `store-ready 0.1.15 validated` |
| Политики | `curl raw.githubusercontent.com/.../privacy.md` | **200**, `terms.md` — **200** |
| Публичность репозитория | `api.github.com/repos/...` | **200** — ссылки из приложения работают |

**По коду не осталось ничего.** Все 10 PR и пункты S1–S8 закрыты. Открытое ниже — либо
владельческие действия, либо то, что я перечислил в разделе 4.

---

## 2. Что закрыто с 17 сентября

| # | Было | Статус |
|---|---|---|
| **O1** | Хост API захардкожен в `src/index.ts` | ✅ **код закрыт** (`eff08f40`) — сборка требует явный `OVERWOLF_API_BASE_URL`, дефолта нет |
| **O2** | Store-ассеты — заглушки | ✅ **закрыто сегодня** — реальное лого в четырёх файлах, `launcher.ico` теперь 6 размеров |
| **O5** | Монетизация | ✅ снято (правило условное), нужен только ответ DevRel |
| **PR 10** | Явный origin | ✅ `eff08f40` |
| — | Политики не опубликованы | ✅ **сейчас публичны** — проверено `curl`, обе 200 |

---

## 3. Что осталось — по исполнителю

### 🔴 Блокеры подачи (за владельцем)

| # | Что | Почему блокер |
|---|---|---|
| **B1** | **Ассеты страницы стора** — плитка 258×198 JPG, иконка 55×55 PNG, hero 258×198, creator 400×320 | В репозитории их **нет ни одного**. Без них листинг не собрать |
| **B2** | **Скриншоты** 1200×675 JPG, до 100 КБ, от 1 до 5 | Ничего не начато. Снимаются руками в матче |
| **B3** | **Текст листинга** — описание до 2000 символов, категория, ключевые слова | Ничего не начато |
| **B4** | **Заявка на идею приложения** через форму Overwolf | В плане Phase 0 стоит **неотмеченной**. Без одобренной заявки приложение считается non-compliant |
| **B5** | **Сборка OPK и отправка** | Процедуры нет вообще: `overwolf-production-release.md` описывает только сайдлоад, а про OPK говорит «intentionally outside the current scope» |
| **B6** | **Ручная матрица** — 1366×720, 1366×768, 1920×1080 @125 DPI, 3840×2160 @150 DPI | Включая проверку alt-tab: на десктопе не должно быть окна приложения, пока игра не в фокусе |

### 🟠 Живой риск (за владельцем)

| # | Что | Проверено |
|---|---|---|
| **B7** | **`CORS_ALLOWED_ORIGINS` не задан в проде** → CORS разрешает **все** origin'ы | ✅ проверено на VPS: переменной нет, `readCorsOrigins()` возвращает `true` |
| **B8** | **Ротация 4 секретов** — `DEADLOCK_API_KEY`, `INTERNAL_API_KEY`, `BUILD_DEBUG_PASSWORD`, `BUILD_DEBUG_SESSION_SECRET` | Не сделано. Ключ к чужому API считается скомпрометированным |
| **B9** | **Внешний приёмник бэкапов** | Дампы лежат на том же диске, что и база — от потери машины не защищают |
| **B10** | **Ответ DevRel**: заявить «без монетизации в v1» и **убрать пустой `#ow-ad-container`** | Контейнер **всё ещё в разметке** (`desktop.html:850`), хотя ревью 17-го рекомендовало выпилить |
| **B11** | **Production-домен** | DuckDNS остаётся боевым хостом. Ревьюер Overwolf увидит динамический DNS домашнего хоста |

### 🟡 Мелочи, которые могу закрыть я

| # | Что | Факт |
|---|---|---|
| **C1** | `minimum-gep-version: "305.0"`, а код читает `match_outcome` (нужен **310.2.8**) | Всё ещё `305.0`. При GEP 305–310.2.7 поле не придёт |
| **C2** | `public/icon.png` — посторонняя картинка (лого «TELEMETRY» с шестерёнкой), JPEG 1024×1024, **565 КБ**, ноль ссылок | Лежит с 9 июля, уедет в OPK мёртвым грузом |
| **C3** | В `.env.example` нет `CORS_ALLOWED_ORIGINS`, `INTERNAL_API_KEY`, `ADAPTIVE_RECOMMENDATIONS_ENABLED` и др. | Проверено: 0 вхождений |
| **C4** | `unpkg.com` и `fonts.googleapis.com` в `externally_connectable` | Внешние зависимости в рантайме оверлея |

---

## 4. Находка, которой не было в ревью 17-го

**Ярлык приложения называется `Statlocker Companion`, а манифест объявляет `Dynamo Lab`.**

`UID` приложения (`dghhkkchkmklmfghdjhibaanfobhkkjeofahanjp`) **выводится из `meta.name` и
`meta.author` и не меняется после публикации**. Финальный гейт требует: «`meta.name` и `meta.author`
финальны и не изменятся после публикации».

Если приложение уже регистрировалось под именем `Statlocker Companion`, то смена имени даст **другой
UID** — то есть для Overwolf это будет **другое приложение**, и вся история публикации потеряется.

**Проверить до подачи:** открыть Developer Console и посмотреть, какой UID там числится. Если
`dghhkkchkmklmfghdjhibaanfobhkkjeofahanjp` — имя менять нельзя, придётся оставить как в консоли.

---

## 5. Порядок действий

Сначала то, что с длинным сроком и не зависит от кода:

1. **Открыть Developer Console** и проверить UID и текущее имя (раздел 4). Это может обнулить
   часть работы, если имя придётся откатывать.
2. **Подать заявку на идею** (B4) — самое долгое по срокам, и без неё остальное бессмысленно.
3. **Задать `CORS_ALLOWED_ORIGINS` в проде** (B7) — одна переменная, а дыра живая прямо сейчас.
4. **Ответить DevRel про монетизацию** и выпилить `#ow-ad-container` (B10).
5. **Нарисовать 4 ассета** (B1) — я могу собрать из них готовые файлы так же, как собрал иконки.
6. **Снять скриншоты** и написать листинг (B2, B3).
7. **Прогнать матрицу разрешений** (B6).
8. **Собрать OPK и отправить** (B5) — процедуру надо написать, её нет.

Параллельно, без спешки: ротация секретов (B8), приёмник бэкапов (B9), домен (B11).

---

## 6. Чего я НЕ проверял

- **DevRel-переписка** — целиком за владельцем, следов в репозитории нет.
- **Developer Console** — доступа нет, поэтому UID и статус заявки не подтверждены.
- **Ручная матрица разрешений** — нужен реальный запуск на четырёх конфигурациях.
- **Сабмит OPK** — процедуры не существует, проверять нечего.
